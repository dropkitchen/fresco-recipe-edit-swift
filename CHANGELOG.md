# Changelog

Every release records the facts needed to reproduce or audit it: artifact size per slice, the
SHA-256 the manifest pins, the toolchain that built it, and the versions of everything compiled in
or linked against. Appended automatically by `fresco-recipe-edit-ios/scripts/publish.sh`.

<!-- releases are appended below this line -->
